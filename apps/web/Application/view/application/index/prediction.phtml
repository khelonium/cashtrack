<div class="row">
    <hr/>

    <div class="col-md-6">
        <table class="table table-hover">
            <th>Name</th> <th class="text-right">Amount</th>
            <?php foreach ($this->accounts as $name => $amount): ?>
                <tr>
                    <td><?php echo $name;?></td><td class="text-right"><?php echo round($amount,2); ?></td>
                </tr>
            <?php endforeach; ?>
            <th>Total</th><th class="text-right"><?php echo round(array_sum($this->accounts),2);?></th>
        </table>
    </div>
</div>